# Security policy

Security fixes are applied to the latest version on `main`.

Do not open a public issue for a vulnerability. Use private vulnerability reporting from the repository's **Security** tab. Never attach sensitive production data; create a minimal synthetic reproduction instead.
